<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dante.diploma">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.UserStepResultsActivity"
            android:label="Итоги курса" />
        <activity
            android:name=".Activities.SignUpActivity"
            android:label="Регистрация"/>
        <activity
            android:name=".Activities.SettingsActivity"
            android:label="Информация об аккаунте"/>
        <activity
            android:name=".Activities.EmailPasswordActivity"
            android:label="Вход в аккаунт">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.StepsActivity"
            android:label="Шаги"/>
        <activity
            android:name=".Activities.TopicsActivity"
            android:label="Темы курса"/>
        <activity
            android:name=".Activities.MainActivity"
            android:label="Доступные курсы"/>
    </application>

</manifest>